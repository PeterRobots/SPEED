# Install the CUDA stack first with one of the versioned files documented in
# README.md, then install these shared dependencies.

# Training, distributed execution, logging, and config.
accelerate>=1.9,<2
diffusers>=0.35,<0.40
transformers>=4.56,<5
omegaconf>=2.3,<2.4
swanlab[dashboard]>=0.7,<1
tqdm
requests
einops

# Data loading and image/video I/O.
numpy<2
pandas
opencv-python<5
Pillow
av
scikit-image<0.26

# Perceptual losses and metrics.
lpips==0.1.4
DISTS-pytorch==0.1
